# Copy this file to .env on your VM at ~/openclaw/.env
# NEVER commit the real .env file.

# OpenClaw Docker settings
OPENCLAW_CONFIG_DIR=/home/YOUR_USER/.openclaw
OPENCLAW_WORKSPACE_DIR=/home/YOUR_USER/.openclaw/workspace
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_BRIDGE_PORT=18790
OPENCLAW_GATEWAY_BIND=lan
OPENCLAW_GATEWAY_TOKEN=      # Generate with: openssl rand -hex 32
OPENCLAW_IMAGE=alpine/openclaw

# API Keys for plugins
ANTHROPIC_API_KEY=            # From console.anthropic.com
OPENAI_API_KEY=               # From platform.openai.com (for mem0 embeddings)
MISTRAL_API_KEY=              # From console.mistral.ai (for Voxtral audio transcription)
